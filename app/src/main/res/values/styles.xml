<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="MainTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/white</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="RecordTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here.<item name="android:windowActionBarOverlay">true</item> -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark_record</item>
        <!-- 去掉ActionBar与Activity之间的蓝色横线 -->
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:actionBarSize">@dimen/actionbar_height</item><!--设置ActionBar的高度-->

        <!-- 兼容支持库 -->
        <item name="windowActionBarOverlay">true</item>
    </style>
    <style name="opinionTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here.<item name="android:windowActionBarOverlay">true</item> -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark_record</item>
        <!-- 去掉ActionBar与Activity之间的蓝色横线 -->
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:actionBarSize">@dimen/actionbar_height</item><!--设置ActionBar的高度-->

        <!-- 兼容支持库 actionBar浮动悬空
        <item name="windowActionBarOverlay">true</item>-->
    </style>

    <style name="detailsTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here.<item name="android:windowActionBarOverlay">true</item> -->
        <item name="colorPrimary">@color/detailsColorPrimary</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark_record</item>
        <!-- 去掉ActionBar与Activity之间的蓝色横线 -->
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:actionBarSize">@dimen/actionbar_height</item><!--设置ActionBar的高度-->

        <!-- 兼容支持库 -->
        <item name="windowActionBarOverlay">true</item>
    </style>




    <!--自定义Dialog样式-->
    <style name="MyDialog" parent="android:style/Theme.Dialog">
        <!--背景颜色及和透明程度-->
        <item name="android:background">@android:color/transparent</item>
        <!--win背景颜色及和透明程度-->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!--win title显示-->
        <item name="android:windowNoTitle">true</item>
        <!--是否浮现在activity之上-->
        <item name="android:windowIsFloating">true</item>
    </style>
    <!--Datepicker样式-->
    <style name="MyDatePicker" parent="@android:style/Widget.Holo.DatePicker">
        <item name="android:textColorPrimary">@color/informationT_color</item>
        <item name="android:divider">@color/informationT_color</item>
    </style>
</resources>
